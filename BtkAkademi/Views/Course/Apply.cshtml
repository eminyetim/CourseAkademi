@model Candidate

<div class="display-6" >
    Apply
</div>

<form method="post" action="/course/apply">
    <div class="form-group my-2"> <!-- m = Margin(dış boşluk) y = y ekseni --> 
            <label>Email</label>
            <input name="Email" class="form-control"/>
    </div>

    <div class="form-group my-2"> <!-- m = Margin(dış boşluk) y = y ekseni --> 
            <label>First Name</label>
            <input name="FirstName" class="form-control"/>
    </div>

    <div class="form-group my-2"> <!-- m = Margin(dış boşluk) y = y ekseni --> 
            <label>Last Name</label>
            <input name="LastName" class="form-control"/>
    </div>

    <div class="form-group my-2">
        <label>Age</label>
        <select name="Age" class="form-control">
            @for(int i = 18 ; i<=23; i++)
            {
                <option value=@i>@i</option>
            }
        </select>
    </div>

    <fieldset>
        <legend>Select One Course</legend>
        <p>
            <input id="mvc" type="radio" name="SelectedCourse" value="Asp.Net Core MVC">
            <label>ASP.NET Core MVC</label>
        </p>
         <p>
            <input id="api" type="radio" name="SelectedCourse" value="Asp.Net Core API">
            <label>ASP.NET Core API</label>
        </p>
         <p>
            <input id="api" type="radio" name="SelectedCourse" value="Blazor">
            <label>Blazor</label>
        </p>
        
    </fieldset>
    
    <div>
        <button class="btn btn-primary" type="submit">Apply</button>
        <a class="btn btn-warning" href="/Home">Home</a>
    </div>

    
</form>